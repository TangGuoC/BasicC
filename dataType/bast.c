#include <stdio.h>

int main ()
{
#if 0
 int age = 5;
 int len = sizeof(age);
 printf("len:%d age = %d\n", len ,age );
#endif
#if 0
 float hight = 230.4;
 len = sizeof(hight);
 printf("len;%d hight = %f\n", len , hight ); 

 double hight = 230.4;
 len = sizeof(hight);
 printf("len;%d hight = %f\n", len , hight );
#endif
#if 0
  char vale = 'm';
  int len = sizeof(vale);
  printf("len:%d vale = %c\n", len , vale );
#endif
#if 0
    long bigValue = 0;
    int len = sizeof (bigValue); 
    printf ("len: %d,bigValue = %ld\n" , len , bigValue);
#endif
#if 0
    short smallValue = 0;
    int len = sizeof (smallValue); 
    printf ("len: %d, smallValue = %d\n" , len , smallValue );
#endif    
#if 1
    

#endif










 return 0;




}
